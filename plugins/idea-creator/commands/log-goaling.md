---
description: Append a dated entry to the goaling diary — current phase, decisions, blockers, next step, in caveman style
argument-hint: [note]
---

Append an entry to the goaling diary `docs/notes/goaling-log.md`.

`$ARGUMENTS` (optional) = a note to fold in. Run **`goal-log`** to append one dated, caveman entry capturing current state: phase, key decisions, blockers, what's next. Record only important notes — skip play-by-play.

Used on demand by the human, and automatically by the pipeline at every phase boundary.
